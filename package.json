{
  "name": "offline-activation-backend",
  "version": "1.0.0",
  "description": "Backend for the Offline Activation Telegram Mini App — Telegram Stars payments via createInvoiceLink",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "set-webhook": "node scripts/setWebhook.js",
    "delete-webhook": "node scripts/deleteWebhook.js",
    "webhook-info": "node scripts/webhookInfo.js",
    "seed-catalog": "node scripts/seedCatalog.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
